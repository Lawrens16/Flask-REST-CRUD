# Flask-REST-CRUD
A Python-based Flask RESTful API providing CRUD functionality for a User, Post, and Comment database schema backed by MySQL. The API is tested and supports data retrieval in both JSON and XML formats.
